const fs = require("fs"); const path = require("path"); const filesToRead = [ "src/App.jsx", "src/store/useStore.js", "paytrack-lite-backend/index.js", "paytrack-lite-backend/src/routes/bookings.js" ]; console.log("=== STARTING DETAILED FILE SCAN ==="); filesToRead.forEach(file => { const filePath = path.join(process.cwd(), file); console.log(`\n=========================================\nFILE: ${file}\n=========================================`); if (fs.existsSync(filePath)) { const content = fs.readFileSync(filePath, "utf8"); console.log(content); } else { console.log("File not found at: " + filePath); } }); console.log("\n=== SCAN COMPLETE ===");
